// M10 fixture: Cadence Perspec SLN scenario (illustrative syntax; format is proprietary).
component counter_system {
    resource counter_res { instance: "top.u_counter"; }

    action load_and_count {
        uses counter_res;
        constraint { cycles in [1..255]; }
    }

    scenario count_up {
        dut: "top";
        do load_and_count;
    }
}
